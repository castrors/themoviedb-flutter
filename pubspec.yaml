name: themoviedb
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  flutter_bloc: ^2.0.0
  equatable: ^0.6.0
  dio: 3.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  mockito: ^4.0.0
  test: any
  bloc_test: ^2.1.0

flutter:
  uses-material-design: true
